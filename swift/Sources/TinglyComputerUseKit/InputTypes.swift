/// Mouse button kind for input simulation.
public enum MouseButtonKind {
    case left
    case right
    case middle
}

/// Scroll direction for input simulation.
public enum ScrollDirectionKind {
    case up
    case down
    case left
    case right
}
